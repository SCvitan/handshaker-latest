server {
    listen 80;
    server_name croworker.app www.croworker.app;

    root /var/www/certbot;

    location /.well-known/acme-challenge/ {
        allow all;
    }

    # Temporary fallback to avoid 403
    location / {
        return 404;
    }
}